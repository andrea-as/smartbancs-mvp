global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: smartbancs-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]

  - job_name: smartbancs-ai
    metrics_path: /metrics
    static_configs:
      - targets: ["ai:8001"]
